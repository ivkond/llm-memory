---
phase: 04-cli
plan: "02"
type: execute
wave: "2"
depends_on:
  - "04-01"
files_modified:
  - packages/cli/src/commands/init.ts
  - packages/cli/src/commands/ingest.ts
  - packages/cli/src/commands/lint.ts
  - packages/cli/src/commands/import-cmd.ts
  - packages/cli/src/commands/search.ts
  - packages/cli/src/commands/status.ts
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - CLI-05
  - CLI-06
user_setup: []

must_haves:
  truths:
    - "llm-wiki init creates wiki directory with git and config"
    - "llm-wiki ingest processes source and returns page path"
    - "llm-wiki lint runs phases and shows report"
    - "llm-wiki import imports from Claude Code memory"
    - "llm-wiki search shows ranked results"
    - "llm-wiki status shows health summary"
  artifacts:
    - path: "packages/cli/src/commands/init.ts"
      provides: "wiki directory creation with git init"
    - path: "packages/cli/src/commands/ingest.ts"
      provides: "source ingestion"
    - path: "packages/cli/src/commands/lint.ts"
      provides: "lint phase execution"
    - path: "packages/cli/src/commands/import-cmd.ts"
      provides: "memory import"
    - path: "packages/cli/src/commands/search.ts"
      provides: "hybrid search"
    - path: "packages/cli/src/commands/status.ts"
      provides: "health status"
  key_links:
    - from: "commands/init.ts"
      to: "@llm-wiki/infra"
      via: "GitVersionControl, FsFileStore"
    - from: "commands/ingest.ts"
      to: "@llm-wiki/common"
      via: "AppServices.ingest"
    - from: "commands/search.ts"
      to: "@llm-wiki/common"
      via: "AppServices.query"
---

<objective>
Implement all 6 CLI commands with full functionality using services from @llm-wiki/common.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-cli/04-CONTEXT.md
@.planning/phases/04-cli/04-RESEARCH.md
@packages/common/src/build-container.ts
@packages/common/src/app-services.ts
@packages/core/src/services/ingest-service.ts
@packages/core/src/services/query-service.ts
@packages/core/src/services/status-service.ts
@packages/core/src/services/lint-service.ts
@packages/core/src/services/import-service.ts
</context>

<read_first>
- packages/cli/src/commands/init.ts (existing stub)
- packages/cli/src/commands/ingest.ts (existing stub)
- packages/cli/src/commands/lint.ts (existing stub)
- packages/cli/src/commands/import-cmd.ts (existing stub)
- packages/cli/src/commands/search.ts (existing stub)
- packages/cli/src/commands/status.ts (existing stub)
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Implement init command</name>
  <files>packages/cli/src/commands/init.ts</files>
  <action>
Implement `llm-wiki init [directory]`:

1. Parse directory argument (default: current dir)
2. Check if wiki already exists (error if .llm-wiki present unless --force)
3. Create directory structure:
   - `wiki/` directory
   - `wiki/pages/` (empty, with .gitkeep)
   - `wiki/projects/` (empty, with .gitkeep)
   - `wiki/.llm-wiki/config.yaml` (default config)
4. Initialize git repo: `git init` in wiki root
5. Create default config file with WikiConfig defaults
6. Output success with wiki path

Use: `GitVersionControl` from infra for git init, `FsFileStore` for directory creation.
  </action>
  <verify>
    <automated>grep -q "GitVersionControl\|git init" packages/cli/src/commands/init.ts</automated>
  </verify>
  <done>llm-wiki init creates wiki directory with git and default config</done>
</task>

<task type="auto">
  <name>Task 2: Implement ingest command</name>
  <files>packages/cli/src/commands/ingest.ts</files>
  <action>
Implement `llm-wiki ingest <source>`:

1. Require wiki directory (must run init first, or pass --wiki path)
2. Load config: ConfigLoader.load() with wiki override
3. Build services: buildContainer(config)
4. Call services.ingest.ingest({ source: args.source })
5. Display result:
   - Success: "Ingested: {page_path}" in green
   - Error: error message in red with suggestion
6. Handle --verbose: show token usage, timing
7. Handle --dry-run: validate without persisting
  </action>
  <verify>
    <automated>grep -q "services.ingest" packages/cli/src/commands/ingest.ts</automated>
  </verify>
  <done>llm-wiki ingest processes source and displays page path</done>
</task>

<task type="auto">
  <name>Task 3: Implement lint command</name>
  <files>packages/cli/src/commands/lint.ts</files>
  <action>
Implement `llm-wiki lint [--phases]`:

1. Require wiki directory
2. Load config and build services
3. Parse --phases (default: all phases)
   - Valid: consolidate, promote, health
4. Call services.lint.run({ phases: [...] })
5. Display report:
   - Table format with phase name, status, files affected
   - Summary: X files consolidated, Y promoted, Z issues
6. Exit code: 0 if no errors, 1 if errors
  </action>
  <verify>
    <automated>grep -q "services.lint" packages/cli/src/commands/lint.ts</automated>
  </verify>
  <done>llm-wiki lint runs phases and displays report</done>
</task>

<task type="auto">
  <name>Task 4: Implement import command</name>
  <files>packages/cli/src/commands/import-cmd.ts</files>
  <action>
Implement `llm-wiki import`:

1. Require wiki directory
2. Load config and build services
3. Parse --agent (default: all, supported: claude-code)
4. Call services.import_.run({ agent: 'claude-code' })
5. Display count: "Imported X entries"
6. Handle no entries: "No entries to import"
  </action>
  <verify>
    <automated>grep -q "services.import_" packages/cli/src/commands/import-cmd.ts</automated>
  </verify>
  <done>llm-wiki import imports from Claude Code memory</done>
</task>

<task type="auto">
  <name>Task 5: Implement search command</name>
  <files>packages/cli/src/commands/search.ts</files>
  <action>
Implement `llm-wiki search <query>`:

1. Require wiki directory
2. Load config and build services
3. Parse --limit (default: 5), --format (default: rich)
4. Call services.query.query({ query: args.query, limit: N })
5. Display results:
   - Rich: numbered list with title + snippet
   - JSON: full result objects
6. Handle no results: "No results found"
  </action>
  <verify>
    <automated>grep -q "services.query" packages/cli/src/commands/search.ts</automated>
  </verify>
  <done>llm-wiki search shows ranked results</done>
</task>

<task type="auto">
  <name>Task 6: Implement status command</name>
  <files>packages/cli/src/commands/status.ts</files>
  <action>
Implement `llm-wiki status`:

1. Require wiki directory
2. Load config and build services
3. Call services.status.status()
4. Display health summary:
   - Page count: X pages, Y projects
   - Index health: OK / stale / empty
   - Verbatim entries: X unconsolidated
   - Last ingest: timestamp
   - Last lint: timestamp
5. Handle --verbose: show detailed diagnostics
  </action>
  <verify>
    <automated>grep -q "services.status" packages/cli/src/commands/status.ts</automated>
  </verify>
  <done>llm-wiki status shows health summary</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI input → services | Untrusted user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 |Injection| CLI args | mitigate | Validate source arg with sanitization service |
| T-04-02 | Denial | lint command | mitigate | Add --timeout flag, limit file count |
| T-04-03 | Info | config paths | accept | XDG paths are per-user, low risk |
</threat_model>

<verification>
- [ ] llm-wiki init creates valid wiki directory
- [ ] llm-wiki ingest returns page path
- [ ] llm-wiki lint shows report
- [ ] llm-wiki import shows count
- [ ] llm-wiki search shows results
- [ ] llm-wiki status shows summary
</verification>

<success_criteria>
- [ ] CLI builds without errors
- [ ] All 6 commands executable
- [ ] Tests pass (if any)
</success_criteria>

<output>
After completion, create `.planning/phases/04-cli/04-02-SUMMARY.md`
</output>