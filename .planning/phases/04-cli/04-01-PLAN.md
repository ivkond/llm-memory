---
phase: 04-cli
plan: "01"
type: execute
wave: "1"
depends_on: []
files_modified:
  - packages/cli/package.json
  - packages/cli/tsconfig.json
  - packages/cli/src/index.ts
  - packages/cli/src/commands/index.ts
  - packages/cli/src/commands/init.ts
  - packages/cli/src/commands/ingest.ts
  - packages/cli/src/commands/lint.ts
  - packages/cli/src/commands/import-cmd.ts
  - packages/cli/src/commands/search.ts
  - packages/cli/src/commands/status.ts
  - pnpm-workspace.yaml
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - CLI-05
  - CLI-06
user_setup: []

must_haves:
  truths:
    - "llm-wiki CLI package exists with commander framework"
    - "Config loads from XDG directories"
    - "All 6 commands are registered subcommands"
    - "CLI can be invoked via npm bin or node"
  artifacts:
    - path: "packages/cli/package.json"
      provides: "CLI package with bin entry"
    - path: "packages/cli/tsconfig.json"
      provides: "TypeScript config"
    - path: "packages/cli/src/index.ts"
      provides: "Main entry with cliffy command tree"
    - path: "packages/cli/src/commands/*.ts"
      provides: "Command implementations"
  key_links:
    - from: "packages/cli/src/index.ts"
      to: "@llm-wiki/common"
      via: "import buildContainer"
    - from: "packages/cli/src/index.ts"
      to: "@llm-wiki/infra"
      via: "import ConfigLoader"
---

<objective>
Set up the CLI package foundation with cliffy framework, configuration loading, and command structure.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-cli/04-CONTEXT.md
@.planning/phases/04-cli/04-RESEARCH.md
@packages/common/src/build-container.ts
@packages/common/src/app-services.ts
@packages/infra/src/config-loader.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLI package structure</name>
  <files>packages/cli/package.json, packages/cli/tsconfig.json, packages/cli/src/index.ts</files>
  <action>
Create the CLI package at `packages/cli/`:

1. `packages/cli/package.json`:
   - name: `@llm-wiki/cli`
   - type: `module`
   - dependencies: `cliffy` (^0.26.0), `@llm-wiki/common`, `@llm-wiki/infra`
   - bin: `{ "llm-wiki": "./dist/index.js" }`
   - exports: `{ ".": "./dist/index.js", "./commands": "./dist/commands/index.js" }`

2. `packages/cli/tsconfig.json`:
   - extends from `../../tsconfig.base.json`
   - references: `../core` and `../infra`
   - outDir: `dist`, rootDir: `src`

3. `packages/cli/src/index.ts`:
   - Main entry with Cliffy Command tree
   - Imports: `buildContainer` from `@llm-wiki/common`, `ConfigLoader` from `@llm-wiki/infra`
   - Registers 6 subcommands: init, ingest, lint, import, search, status
   - Each subcommand imports from `./commands/`
  </action>
  <verify>
    <automated>test -f packages/cli/package.json && test -f packages/cli/tsconfig.json && test -f packages/cli/src/index.ts</automated>
  </verify>
  <done>CLI package exists with cliffy dependency and bin entry</done>
</task>

<task type="auto">
  <name>Task 2: Create command directory structure</name>
  <files>packages/cli/src/commands/index.ts, packages/cli/src/commands/init.ts, packages/cli/src/commands/ingest.ts, packages/cli/src/commands/lint.ts, packages/cli/src/commands/import-cmd.ts, packages/cli/src/commands/search.ts, packages/cli/src/commands/status.ts</files>
  <action>
Create the commands directory with stub implementations:

1. `packages/cli/src/commands/index.ts`:
   - Re-exports all commands
   - Type: `CommandModule` interface

2. `packages/cli/src/commands/init.ts`:
   - Command: `llm-wiki init [directory]`
   - Options: `--force` (overwrite existing)
   - Handler: stub calling IngestService patterns
   - Imports: `@llm-wiki/infra` for directory creation

3. `packages/cli/src/commands/ingest.ts`:
   - Command: `llm-wiki ingest <source>`
   - Options: `--verbose`, `--dry-run`
   - Handler: stub calling IngestService.ingest

4. `packages/cli/src/commands/lint.ts`:
   - Command: `llm-wiki lint [--phases]`
   - Options: `--phases` (comma-separated: consolidate,promote,health)
   - Handler: stub calling LintService.run

5. `packages/cli/src/commands/import-cmd.ts`:
   - Command: `llm-wiki import`
   - Options: `--agent` (claude-code, all)
   - Handler: stub calling ImportService.run

6. `packages/cli/src/commands/search.ts`:
   - Command: `llm-wiki search <query>`
   - Options: `--limit`, `--format` (rich, json)
   - Handler: stub calling QueryService.query

7. `packages/cli/src/commands/status.ts`:
   - Command: `llm-wiki status`
   - Options: `--verbose`
   - Handler: stub calling WikiStatusService.status
  </action>
  <verify>
    <automated>ls packages/cli/src/commands/*.ts | wc -l</automated>
  </verify>
  <done>All 6 command stubs exist with cliffy decorators</done>
</task>

<task type="auto">
  <name>Task 3: Add CLI to workspace</name>
  <files>pnpm-workspace.yaml</files>
  <action>
Update workspace configuration:

1. Add `@llm-wiki/cli` to `packages/` list in `pnpm-workspace.yaml`

2. Update root `tsconfig.json`:
   - Add reference to `packages/cli`
  </action>
  <verify>
    <automated>grep -q "cli" pnpm-workspace.yaml</automated>
  </verify>
  <done>CLI package registered in workspace</done>
</task>

</tasks>

<verification>
- [ ] CLI package builds without errors
- [ ] All commands are registered in cliffy tree
- [ ] Workspace builds include CLI
</verification>

<success_criteria>
- [ ] `llm-wiki --help` displays 6 subcommands
- [ ] Each command has `--help` showing options
</success_criteria>

<output>
After completion, create `.planning/phases/04-cli/04-01-SUMMARY.md`
</output>